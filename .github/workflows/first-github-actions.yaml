name: Run Shell Commands
on: 
  workflow_dispatch:
    inputs:
      environment:
        description: "请输入要部署的环境"
        required: true
        default: "staging"
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Test
        run: echo "job1"
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo build
        run: echo "job2"
